name: resource_reaper
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.2

dependencies:
  clock: ^1.1.2

dev_dependencies:
  fake_async: ^1.3.3
  lints: ^5.0.0
  test: ^1.24.0
